# 실기기/시뮬레이터에서 localhost는 "폰 자신"이라 백엔드에 닿지 않는다.
#   실기기·iOS 시뮬레이터 → 맥의 LAN IP (터미널에서 `ipconfig getifaddr en0`)
#   Android 에뮬레이터    → http://10.0.2.2:8080
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:8080

# 기록 상세의 "링크 복사"가 만들 웹 주소 (앱이 아니라 웹 클라이언트를 가리킨다)
EXPO_PUBLIC_SITE_URL=http://localhost:3000
