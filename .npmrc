# React Native's Metro bundler and the autolinking that builds the native projects both
# resolve native modules by walking node_modules, which pnpm's symlinked layout breaks.
# Expo's monorepo guide prescribes a hoisted layout for exactly this reason. It applies
# to the whole workspace, which the web app is indifferent to.
node-linker=hoisted
